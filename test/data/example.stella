language core;
extend with #multiparameter-functions ;

fn twice(k : fn(Nat) -> Nat) -> fn(Nat) -> Nat {
  return fn(x : Nat) {
    return k(k(x))
  }
}
fn f(x : Bool, y : Nat) -> Bool { return x }


